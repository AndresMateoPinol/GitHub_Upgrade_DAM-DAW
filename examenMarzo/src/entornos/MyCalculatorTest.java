package entornos;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/**
 * Clase de tests para la clase MyCalculator.
 */
public class MyCalculatorTest {

    // Al estar usando junit, que no está instalado, te saldrán varios errores. 
    // Tú no te preocupes: lo importante es crear los tests con los assets adecuados.
    // De todas formas, os he preparado un archivo para que podáis ejecutar los tests (si queréis).
    // Arrastrad el .jar a la carpeta "lib". Luego, pulsad F1 y escribid "Developer: Reload Window"
}
